baseURL = 'https://EJJE44.github.io/school-library/'
languageCode = 'ru-ru'
title = 'Школьная Библиотека(часть художественной литературы)'
theme = 'hugo-book'

[params]
  # Включает поиск по сайту (очень важно для библиотеки!)
  BookSearch = true
  # Разрешает HTML в markdown (полезно для вставок таблиц)
  BookComments = false

